# PronoHippique AI v2.1 — Dépendances
# ⚠️ VERSIONS CORRIGÉES pour Streamlit Cloud (Python 3.14)

# Streamlit & UI
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.0.0

# Image processing (OBLIGATOIRE pour OCR v2.1)
Pillow>=9.0.0
opencv-python-headless>=4.5.0

# OCR APIs (Cloud) — Compatible Python 3.14
google-generativeai>=0.3.0
openai>=1.0.0

# OCR Fallback local (Recommandé)
easyocr>=1.7.0

# OCR Tesseract (Optionnel, meilleur local si installé système)
pytesseract>=0.3.10

# Excel export — VERSION RÉELLE (max 3.1.5 sur PyPI)
openpyxl>=3.1.0,<3.2.0

# Utilitaires
scikit-image>=0.18.0
requests>=2.28.0
python-dotenv>=0.20.0

# Optional: Meilleure gestion des couleurs
imageio>=2.25.0

# Tools avancés (optionnels)
Werkzeug>=2.0.0
